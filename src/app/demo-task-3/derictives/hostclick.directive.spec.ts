import { HostclickDirective } from './hostclick.directive';

describe('HostclickDirective', () => {
  it('should create an instance', () => {
    const directive = new HostclickDirective();
    expect(directive).toBeTruthy();
  });
});
